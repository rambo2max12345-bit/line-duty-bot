# Copy this to .env (or set environment variables in your hosting platform)
CHANNEL_ACCESS_TOKEN=YOUR_LINE_CHANNEL_ACCESS_TOKEN
CHANNEL_SECRET=YOUR_LINE_CHANNEL_SECRET
# FIREBASE_CREDENTIALS_JSON must be the entire service account JSON as a single-line value.
# Example: export FIREBASE_CREDENTIALS_JSON="$(jq -c . service-account.json)"
FIREBASE_CREDENTIALS_JSON=
ADMIN_LINE_ID=your_admin_line_user_id_or_comma_separated_ids
ADMIN_API_KEY=some-secret-api-key
FONT_FILENAME=Sarabun-Regular.ttf
PORT=5000